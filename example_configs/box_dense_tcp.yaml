# This is the minimal working config. Because not specified otherwise, all runs in the project
# that include the field "test_acc" will be exported as a numpy array to the directory "wandb_data".

experiment1:
  fields: ["evaluation_is_success_mean", "num_global_steps"]
  entity: gelikit

  project: box_random_tcp_entire

  config:
    sampler.args.env_id:
          values: ["BoxPushingRandomInitDenseProDMPTCP-v0"]
    agent.args.lr_policy:
      values: [ 3e-4 ]
    agent.args.lr_critic:
      values: [ 1e-3 ]
    sampler.args.episodes_per_train_env:
          values: [4]
  groups: ["TCP, dense, final, 20 seeds"]
  output_path: /home/lige/Codes/wandb2numpy/wandb_data/box_dense_tcp_prodmp
