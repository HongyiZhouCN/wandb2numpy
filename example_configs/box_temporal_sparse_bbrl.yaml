# This is the minimal working config. Because not specified otherwise, all runs in the project
# that include the field "test_acc" will be exported as a numpy array to the directory "wandb_data".

experiment1:
  fields: ["evaluation_is_success_mean", "num_global_steps"]
  entity: gelikit
#  project: box_temporal_sparse_tcp_entire
  project: box_random_bbrl_entire

  config:
    sampler.args.env_id:
          values: ["BoxPushingRandomInitTemporalSparseProDMP-v0"]
    projection.args.mean_bound:
      values: [ 0.05 ]
    projection.args.cov_bound:
      values: [ 0.001 ]
    sampler.args.episodes_per_train_env:
      values: [ 2 ]
    policy.args.variance_net_args.std_only:
      values: [false]
  groups: ["BBRL, tune cov TR bound, 76 / 152 ep"]
  output_path: /home/lige/Codes/wandb2numpy/wandb_data/box_temporal_sparse_bbrl_prodmp
